import { AutofillMonitorDirective } from './autofill-monitor.directive';

describe('AutofillMonitorDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofillMonitorDirective();
    expect(directive).toBeTruthy();
  });
});
